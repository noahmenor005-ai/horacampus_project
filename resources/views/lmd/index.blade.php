@extends('layouts.app')
@section('title',$title)
@section('content')<div class="d-flex justify-content-between align-items-center mb-3"><div><h1 class="h3 mb-0">{{ $title }}</h1><small class="text-muted">Gestion académique LMD</small></div><a class="btn btn-primary" href="{{ route('lmd.create',$resource) }}">+ Ajouter</a></div><form class="mb-3"><input class="form-control" name="q" value="{{ request('q') }}" placeholder="Rechercher par nom…"></form><div class="card p-3 table-responsive"><table class="table align-middle"><thead><tr>@foreach(array_keys($fields) as $field)<th>{{ ucfirst(str_replace('_',' ',$field)) }}</th>@endforeach<th></th></tr></thead><tbody>@forelse($items as $item)<tr>@foreach(array_keys($fields) as $field)<td>{{ is_bool($item->$field) ? ($item->$field?'Oui':'Non') : $item->$field }}</td>@endforeach<td class="text-end"><a class="btn btn-sm btn-outline-primary" href="{{ route('lmd.edit',[$resource,$item->id]) }}">Modifier</a><form class="d-inline" method="POST" action="{{ route('lmd.destroy',[$resource,$item->id]) }}">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger" onclick="return confirm('Supprimer cet enregistrement ?')">Supprimer</button></form></td></tr>@empty<tr><td colspan="99" class="text-center text-muted">Aucune donnée.</td></tr>@endforelse</tbody></table>{{ $items->links() }}</div>@endsection
